[build-system]
requires = ["setuptools>=77.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cryolens"
version = "0.1.0"
description = "CryoLens: a generative model for reconstructing structures from cryoET tomograms"
authors = [
    { name = "Kyle Harrington", email = "kharrington@chanzuckerberg.com" },
    { name = "Alan Lowe", email = "alowe@chanzuckerberg.com" }
]
requires-python = ">=3.11,<3.13"
readme = "README.md"
license = "MIT"
dependencies = [
    # Core dependencies for training
    "numpy>=1.23.0",
    "torch>=2.0.0,<2.5",
    "pytorch-lightning>=2.0.0,<3.0",
    
    # Scientific computing
    "scipy>=1.10.0",
    "pandas>=2.0.0",
    
    # CryoEM/structural biology
    "mrcfile>=1.5.0",
    "gemmi>=0.7.0",
    "copick>=1.17.0",
    
    # ML infrastructure
    "mlflow>=2.21.3",
    "tqdm>=4.66.0",
    
    # Utilities
    "h5py>=3.15.1",
    "filelock>=3.12.0",
    "pooch>=1.8.0",
]

[project.optional-dependencies]
# Visualization (not needed for training)
viz = [
    "matplotlib>=3.5.0",
    "seaborn>=0.13.2",
    "scikit-image>=0.20.0",
]

# Analysis tools
analysis = [
    "scikit-learn>=1.0.0",
    "tabulate>=0.9.0",
    "graphviz>=0.20.3",
]

# Copick extras
copick = [
    "zarr>=2.16.0",
]

# Development
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.11.10",
]

# Everything
all = [
    "cryolens[viz,analysis,copick,dev]",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
cryolens = ["data/copick_configs/*.json", "data/*.h5"]

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
    "pytest>=8.3.5",
    "pytest-cov>=6.1.1",
    "ruff>=0.11.10",
]
